# Local-only exploration / investigation artifacts — kept out of the repo.
# (`*.mp4` is intentionally NOT here — tracked video assets land in LFS via
# .gitattributes. Local renders stay out via the .weave-cli/ and exploration/
# directory ignores above.)
.weave-cli/
exploration/
.superpowers/
.DS_Store

# Local planning docs (not part of the distribution surface).
2026-05-27-weave-knowledge-satellite.md
